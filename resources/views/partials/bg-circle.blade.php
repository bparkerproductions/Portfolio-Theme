<div 
  class="bg-circle {{ APP::bgCircleClasses($properties) }}"
  style="{{APP::bgCircleStyles(
    $opacity ?? false,
    $posRight ?? false,
    $posTop ?? false,
    $posLeft ?? false
  )}}"
></div>